## 1. Popup copy

- [ ] 1.1 Replace the primary heading in `src/popup/index.html` with `Web 开发者工具 · 示例` and verify the popup markup still passes `npm test`.
- [ ] 1.2 Run `npm run check` and verify manifest validation, static review, and all unit tests pass.
